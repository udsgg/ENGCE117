```c++
#include <stdio.h>

int *GetSet(int *n);

int main() {
    int *data, num;
    data = GetSet(&num);
    if(scanf("%d", &num) != 1 || num <= 0) {
        return 1;
    }
    int arr[num];
    GetSet(&num);
    return 0;
}

int *GetSet(int *n) {
    printf("Enter the size of the array: ");
    if(scanf("%d", n) != 1 || *n <= 0) {
        return nullptr;
    }
    static int arr[100]; 
    for(int i = 0; i < *n; i++) {
        printf("Enter member value [%d]: ", i + 1);
        scanf("%d", &arr[i]);
    }
    for(int i = 0; i < *n; i++) {
        printf("Member %d: %d\n", i + 1, arr[i]);
    }
    return arr;
}
```
