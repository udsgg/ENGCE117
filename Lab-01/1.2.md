```c++
#include <stdio.h>

void GetSet( int arr[] , int *n ) ;

int main() {
    int n ;
    int arr[n] ; 
    printf("Enter the size of the array: ");
    if(scanf( "%d" , &n ) != 1 || n <= 0) {
        return 1 ;
    }
    GetSet( arr , &n ) ;
    return 0 ;
}

void GetSet( int arr[] , int *n ) {
    for( int i = 0 ; i < *n ; i++ ) {
        printf( "Enter member value [%d]: " , i + 1 ) ; 
        scanf( "%d" , &arr[i] ) ;
    }
    for( int i = 0 ; i < *n ; i++ ) {
        printf("Member %d: %d\n" , i + 1, arr[i] ) ;
    }
}
```
