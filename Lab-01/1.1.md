```C++
#include <stdio.h>

int main() {
    int time;
    int arr[time];
    if(scanf("%d", &time) != 1) {
        return 1;
    }   
    for (int i = 1; i <= time; i++) {
        printf("[%d] Hello world\n", i, &arr[i]);
    }
    return 0;
}
```
