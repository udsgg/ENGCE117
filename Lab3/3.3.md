```c
#include <stdio.h>
#include <string.h>

struct student {
    char name[20];
    int age;
    char sex;
    float gpa;
};

struct student upgrade(struct student child);

int main() {
    struct student aboy;
    aboy.age = 20;
    printf("Enter sex (M/F): ");
    scanf("%c", &aboy.sex);
    if (aboy.sex == 'M') {
    strcpy(aboy.name, "Somchai");
    }
    else if (aboy.sex == 'F') {
        strcpy(aboy.name, "Somying");
    }
    aboy.gpa = 3.00;
    aboy = upgrade(aboy);
    printf("%.2f\n", aboy.gpa);   
    printf("upgrade %.2f", aboy.gpa);
    return 0;
}

struct student upgrade(struct student child) {
    if (child.sex == 'M') {
        child.gpa = child.gpa * 1.10;
    } else if (child.sex == 'F') {
        child.gpa = child.gpa * 1.20;
    }
    return child;
}
```
