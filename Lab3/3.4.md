```c
#include <stdio.h>
#include <string.h> 

struct student {
    char name[20];
    int age;
};

void GetStudent(struct student child[][20], int *room);

int main() {
    struct student children[10][20]; 
    int group;
    for(int x = 0; x < 10; x++) {
        for(int y = 0; y < 20; y++) {
            children[x][y].age = -1; 
        }
    }
    GetStudent(children, &group);
    printf("\n--- Student List ---\n");
    for (int i = 0; i < 10; i++) {
        for (int j = 0; j < 20; j++) {
            if (children[i][j].age == 0) {
                printf("Group %d, ID %d: Name: %s\n", i + 1, j + 1, children[i][j].name);
            }
        }
    }
    return 0;
}

void GetStudent(struct student child[][20], int *room) {
    int group, id;
    char name[20];
    for (int i = 0; i < 10; i++) { 
        printf("Enter group (1-10) and id (1-20): ");
        scanf("%d %d", &group, &id);
        printf("Enter name: ");
        scanf("%s", name);
        strcpy(child[group - 1][id - 1].name, name);
        child[group - 1][id - 1].age = 0; 
    }
}
```
